/* Write your global styles here, in PostCSS syntax */
@tailwind base;
@tailwind components;
@tailwind utilities;

html {
  @apply h-full;
}

body {
    font-family: 'Outfit Variable', sans-serif;
    @apply h-full;
}

#app {
    @apply h-full;
}